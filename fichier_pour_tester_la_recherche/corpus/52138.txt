 Style for football tableslocal pp function pp header	 Load relevant modules	local yesno require		 Create table header	 Pre stuff	local team width Args teamwidth or 	local sort text 	local sort table val Args sortable table 	or no 	if yesno then sort text sortable end	table insert 			 Open table		 Custom header options	local group head text Args group header or 	local OTwin head text Args OTwin header or 	local OTloss head text Args OTloss header or 	 Use points instead of goals for against	local for against style Args for against style 	or goals 	local fa letter fa word sing fa word plur	 First convert to lower case if it is a string	for against style string lower	if for against style