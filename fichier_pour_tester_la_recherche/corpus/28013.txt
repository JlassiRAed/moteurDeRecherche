The Template numsense calculates the precision of a number as being rounded to the nearest unit as precision Multiples of are treated as rounded to nearest with precision Examples In decimal precision the number is treated as between rather than Consequently numsense gives the precision same as the numbers Also numerals with trailing zeroes count the extra zeroes as higher precision so has precision for the decimal places 