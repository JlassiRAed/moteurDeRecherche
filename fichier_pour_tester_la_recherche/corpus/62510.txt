This template is used for showing a userbox and its properties in a table row The only mandatory parameter is Wrap around uses of this template Include the full For a userbox that uses up to unnamed parameters If your userbox accepts more parameters make a request on the Template talk Usbk talk page to expand this template For a userbox set that uses up to named parameters If the userbox adds user pages into a user category under 