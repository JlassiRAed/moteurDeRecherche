In computer programming a header file can be thought of as a dictionary a compiler uses if it comes across a word it does not understand Mainly programming language s like C C and C use this As you may know a computer is really powerful But it lacks the power to think and learn As a result a computer has to be fed each and every instruction for a Task that has been mentioned by the programmer If it finds what to do when someone says cout the program will be compiled properly Otherwise it will simply result in an error that so and so word has not been understood by the compiler tech stub 