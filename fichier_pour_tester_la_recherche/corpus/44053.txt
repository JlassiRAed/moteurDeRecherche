 Style for football tableslocal pp function pp header	 Load relevant modules	local yesno require		 Create table header	 Pre stuff	local team width Args teamwidth or 	local sort text 	local sort table val Args sortable table 	or no 	if yesno then sort text sortable end	table insert 			 Open table		 Custom header options	 Table header	local table header Args table header 	if table header then		table insert	end	 Column headers	local pld head text Args pld header or 	local OTloss head text Args OTloss header or 	local tiebr head text Args tiebr header or 	local group head text Args group header or 	 Use points instead of goals for against	local for against style Args for against style 	or goals 	local fa letter fa word sing fa word plur	 First convert to lower case if it is a string	for against style string lower	if for against style