 From Plastikspork s scriptfunction autoEdTablestoWikitext MAIN FUNCTION describes list of fixes Remove newlines from inside table specific tags var loopcount while str replace loopcount Remove extra whitespace from inside table specific tags str str replace str str replace Remove any extra junk str str replace Remove space whitespace after str str replace Remove space before str str replace Replace with str str replace Replace with str str replace Replace with str str replace str str replace Replace with str str replace str str replace Replace with EOL str str replace Replace with EOL str str replace Replace with str str replace str str replace Replace with str str replace str str replace str str replace str str replace Replace with str str replace str str replace str str replace str str replace return str source