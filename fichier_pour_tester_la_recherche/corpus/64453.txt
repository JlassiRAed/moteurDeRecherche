 Style for football tableslocal pp function pp header	 Load relevant modules	local yesno require		 Create table header	 Pre stuff	local team width Args teamwidth or 	local sort text 	local sort table val Args sortable table 	or no 	if yesno then sort text sortable end	table insert 			 Open table		 Custom header options	local draw head text Args draw header or 	local group head text Args group header or 	local team head text Args team header or Team 	local loss first Args loss before draw or false	 What to rank the teams by	local ranking style Args ranking style or pts 	local show points show perc false	ranking style string lower	if ranking style