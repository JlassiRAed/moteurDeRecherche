In the Domain Name System an A record is something that changes a domain name into a bit IP address A domain can have many A records for example the domain example com could have an A record for www example com that point s to the IP and another A record for mail example com that points to the IP 