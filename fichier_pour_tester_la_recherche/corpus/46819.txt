 Convert HTML to wikitextfunction autoEdHTMLtoWikitext tags str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace Not really an HTML to wikitext fix but close enough str str replace Repeated references tag str str replace Make sure is after a newline str str replace Make sure is before a newline str str replace Remove newlines from inside var loopcount while str str replace loopcount Replace with wikified section headings str str replace str str replace gim 