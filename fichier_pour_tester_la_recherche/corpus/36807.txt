 This module implements local p local function isnotempty	return s and s match endfunction p table	local args args	local entries 	local colclass 	local colstyle 	local cols tonumber or 	 create the root table	local root mw html create	 add table style for fullwidth	if isnotempty then		root			 css			 css			 css			 css	end	 add table classes	if isnotempty then		root addClass	end	 add table style	if isnotempty then		root cssText	end	 build arrays with the column styles and classes	if isnotempty then		colstyle text align left 		colstyle text align right 	end	for i cols do		colclass i colclass i or 		colstyle i colstyle i or 		if isnotempty then			colstyle i args colstyle colstyle i 		end		if isnotempty then			colstyle i text align args colalign tostring colstyle i 		elseif isnotempty then			colstyle i text align args col tostring align colstyle i 		elseif isnotempty then			colstyle i text align args align tostring colstyle i 		end		if isnotempty then			colstyle i white space nowrap colstyle i 		elseif isnotempty then			colstyle i white space nowrap colstyle i 		elseif isnotempty then			colstyle i white space nowrap colstyle i 		end		if isnotempty then			colstyle i width args colwidth tostring colstyle i 		elseif isnotempty then			colstyle i width args col tostring width colstyle i 		elseif isnotempty then			colstyle i width args colwidth colstyle i 		end		if isnotempty then			colstyle i colstyle i args colstyle tostring 		elseif isnotempty then			colstyle i colstyle i args col tostring style 		elseif isnotempty then			colstyle i colstyle i args style tostring 		end		if isnotempty then			colclass i args colclass tostring 		elseif isnotempty then			colclass i args col tostring class 		elseif isnotempty then			colclass i args class tostring 		end	end	 compute the maximum cell index	local cellcount 	for k v in pairs do		if type 