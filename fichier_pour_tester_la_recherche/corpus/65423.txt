 NOTEThis template is an implementation detail encapsulating the color association we use for its parent template It has a single unnamed parameter and expands to if a name exists among the names supported by the HTML standard otherwise No final semicolon is included ever noinclude