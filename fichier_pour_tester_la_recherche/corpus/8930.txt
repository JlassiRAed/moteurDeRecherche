 function autoEdUnicodify MAIN FUNCTION describes list of fixes Task Replace named html entities with unicode Most common replacements str str replace str str replace Case insensitive symbols if XML and HTML Symbols str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace Uppercase symbols if Greek symbols str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace Latin symbols str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace XML and HTML Symbols str str replace str str replace lowercase symbols if Greek symbols str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace Latin symbols str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace XML and HTML Symbols str str replace str str replace False positives Breaks large amounts of code which discuss programming scripting str str replace str str replace Breaks large number of URLs and discussion of programming scripting str str replace Arrows str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace 