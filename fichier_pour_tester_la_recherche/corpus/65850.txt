This category contains utility templates used to compute dates and time values Despite their internal design they are built for maximum reusability in various contexts so the syntax to use them is very simple and each of these templates is documented with examples of use and return values These templates are useful for auto categorization or for creating navigation panels in articles the names of which include date elements 