 This is a meta module for producing message box templates including Load necessary modules requirelocal getArgslocal categoryHandler require mainlocal yesno require Get a language object for formatDate and ucfirst local lang mw language getContentLanguage Define constantslocal CONFIG MODULE Module Message box configuration Helper functions local function getTitleObject	 Get the title object passing the function through pcall	 in case we are over the expensive function count limit 	local success title pcall	if success then		return title	endendlocal function union	 Returns the union of two arrays 	local vals 	for i v in ipairs do		vals v true	end	for i v in ipairs do		vals v true	end	local ret 	for k in pairs do		table insert	end	table sort	return retendlocal function getArgNums	local nums 	for k v in pairs do		local num mw ustring match		if num then			table insert		end	end	table sort	return numsend Box class definition local MessageBox MessageBox index MessageBoxfunction MessageBox new	args args or 	local obj 	 Set the title object and the namespace 	obj title getTitleObject	 Set the config for our box type 	obj cfg cfg boxType 	if not obj cfg then		local ns obj title namespace		 boxType is mbox or invalid input		if ns 