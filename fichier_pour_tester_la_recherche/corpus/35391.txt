 function autoEdFullwidth MAIN FUNCTION describes list of fixes Spacing periods and interpucts str str replace fullwidth space str str replace fullwidth interpunct str str replace str str replace three interpuncts normally mean an ellipsis str str replace catch already converted triple interpuncts it s after the interpunct rule so we only need one go through to catch partial replacements str str replace Common punctuation str str replace str str replace str str replace str str replace Numbers str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace Latin letters uppercase str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace Latin letters lowercase str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace Other punctuation str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace this replacement could break template usage use as an alternative inside templates str str replace str str replace str str replace this replacement could break wikimarkup usage str str replace str str replace this replacement could break wikimarkup usage str str replace str str replace str str replace str str replace this replacement could break wikimarkup usage use as an alternative str str replace this replacement could break template usage use as an alternative inside templates str str replace this replacement could break wikimarkup usage use as an alternative str str replace ASCII tilde is apparently treated as a diacritic in some fonts so this should be a better replacement str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace str str replace return str source