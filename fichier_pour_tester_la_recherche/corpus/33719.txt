 URLs must begin with a supported URI scheme will require a plug in or an external application and should normally be avoided IPv host names are currently not supported If URLs in citation template parameters contain certain characters then they will not display and link correctly Those characters need to be percent encoded For example a space must be replaced by To encode the URL replace the following characters with noinclude